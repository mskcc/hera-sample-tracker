export const BASE_URL = "https://delphi.mskcc.org/sample-tracker-dev-api/"
export const BASE_ROUTE = "/sample-tracker-dev"   //this is needed when routing from one page to another
export const ADMIN_EMAIL = 'skicmopm@mskcc.org'
export const requestHeaders = {
    headers: {
        'Content-Type': 'application/json',
        'Access-Control-Allow-Origin': '*',
    }
}